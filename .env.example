# SigNoz workspace (required for real tool calls; optional at container start in HTTP mode)
SIGNOZ_URL=https://your-signoz-instance.com
SIGNOZ_API_KEY=your-api-key

# HTTP MCP (defaults match the image)
TRANSPORT_MODE=http
MCP_SERVER_PORT=8000
LOG_LEVEL=info
